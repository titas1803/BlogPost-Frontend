import styled from "styled-components";

export const HeaderStyle = styled.header`
  nav {
    padding: 0;

    .navbar-brand {
      img {
        width: 200px;
        height: auto;
      }
    }
  }
`;
